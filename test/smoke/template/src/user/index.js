import './index.scss';
import ReactDOM from 'react-dom';
// eslint-disable-next-line no-unused-vars
import React from 'react';
// eslint-disable-next-line no-unused-vars
import App from './app';

ReactDOM.render(
  <App context={'hello word'}/>,
  document.getElementById('app'),
);
